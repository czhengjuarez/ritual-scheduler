/**
 * TeamRitualAudit's deployed origin. Plain external link only — no shared
 * session, no cross-app API calls (see intent-native-front-door decision,
 * PLAN.md §1.5). Safe to hardcode: it's a public URL, not a secret.
 */
export const TEAM_RITUAL_AUDIT_URL = "https://team-ritual-audit.coscient.workers.dev";
