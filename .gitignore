# dependencies
node_modules/

# build output
dist/

# cloudflare / wrangler
.wrangler/
.dev.vars
.dev.vars.*
!.dev.vars.example

# generated
worker-configuration.d.ts
*.tsbuildinfo

# logs
*.log
npm-debug.log*

# editor / os
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/

# env
.env
.env.*
!.env.example

# Google OAuth client id (public-safe value, but keep the pattern identical
# to TeamRitualAudit so the suite's one shared client id lives the same way
# in both repos — see suite-auth-strategy memory).
src/config/google-auth.ts
!src/config/google-auth.example.ts
